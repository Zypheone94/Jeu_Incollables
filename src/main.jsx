import React from 'react'
import ReactDOM from 'react-dom'
import App from './App'


ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
)

// Fichier principal de notre projet, il est celui qui va faire tourner notre application, il n'y a aucune raison de le modifier